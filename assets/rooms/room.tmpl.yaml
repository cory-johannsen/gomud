name: {{ .name }}
description: |
  {{ .description }}
exits:
  {% range .exits %}
  - {{ .direction }}:
    name: {{ .name }}
    description: |
      {{ .description }}
    target: {{ .target }}
  {% end %}